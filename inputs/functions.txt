// This file's path is added as a cli argument therefore code within here executes.

// Current errors:
// logging arrow and position is off with error printing

func foo() -> void {
	outln "Hello World!";
}

func add(x -> number, y -> number) -> void {
	outln x + y;
}

func r(x -> number) -> number {
    return x;
}

foo();
add(1, 2);
outln r(20);

var a -> string[] = [1,2,3]; // errornous

outln typeof a;
